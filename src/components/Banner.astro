---
import { getImage } from "astro:assets"
import starsBG from "../assets/dana-luig.jpg"

// Process the image
const optimizedBackground = await getImage({
	src: starsBG,
	format: "webp",
})
---

<section class="py-36 relative">
	<div class="absolute inset-0 bg-fixed bg-center bg-cover bg-no-repeat" style={`background-image: url(${optimizedBackground.src});`}></div>
	<div class="absolute inset-0 bg-black/50"></div>
	<div class="relative container px-4 mx-auto">
		<div class="mx-auto mb-20 text-center">
			<h2 class="mt-8 text-6xl lg:text-7xl font-bold font-heading text-white font-title">Boldly Selling Where No One Has Sold Before!</h2>
		</div>
	</div>
</section>

<style>
	/* Optional: Add additional parallax enhancement */
	.bg-fixed {
		background-attachment: fixed;
	}
</style>
